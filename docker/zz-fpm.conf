[www]
; php-fpm clears the environment by default, which would make getenv() in
; app/core/config.php return false. Keep the container's env vars (DB_HOST,
; DB_USER, DB_PASSWORD, ...) visible to PHP scripts.
clear_env = no
